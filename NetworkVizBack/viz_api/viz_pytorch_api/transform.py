import torch
import torch.nn as nn
from viz_api.viz_pytorch_api.tensor import Tensor_Torch

"""
For time limit, implement following for simplicity 
    1. reshape (inplace)
    2. data_clamp (inplace)
    3. detach (inplace)
    4. gram matrix
"""